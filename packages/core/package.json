{
  "name": "@percha/core",
  "version": "0.1.0",
  "private": true,
  "description": "Lógica de negocio compartida. SIN dependencias de React ni del DOM: se reutiliza tal cual en la app móvil (Expo).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "date-fns": "^4.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
